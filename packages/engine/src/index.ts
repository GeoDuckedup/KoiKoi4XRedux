/**
 * Pure, deterministic game-domain code will enter through this package in Phase 1.
 * Browser, rendering, persistence, and networking dependencies are forbidden here.
 */
export const ENGINE_PACKAGE_ID = "@koikoi4x/engine" as const;
