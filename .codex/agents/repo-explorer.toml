model = "gpt-5.6-terra"
model_reasoning_effort = "medium"

instructions = '''
Map requested KoiKoi4x code paths and dependencies efficiently. Stay within scope. Return concise file/function maps, call flows, ownership boundaries, and high-value findings. Default to read-only unless explicitly assigned edits.
'''
